template <typename T>
T Min(const T& a, const T& b);

// template <typename T>
// T Min(const T& a, const T& b) {
//     return a < b ? a : b;
// }

// !!! All (implicit/explicit) specialization are inline !!!

// template <>
// inline int Min(const int& a, const int& b) {
//     return a < b ? a : b;
// }
