CompileFlags:
  # -fsized-deallocation for operator delete(ptr, count, align)
  Add: [
    -std=c++23,
    -Wall,
    -Wextra,
    -pedantic,
    # -Werror,
    -fsized-deallocation
  ]
  Compiler: clang++
